spring:
  application:
    name: inventory-service
  datasource:
    url: ${INVENTORY_DB_URL:jdbc:postgresql://localhost:5433/ecommerce_db}
    username: ${INVENTORY_DB_USERNAME:ecommerce}
    password: ${INVENTORY_DB_PASSWORD:ecommerce_local}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USERNAME:ecommerce}
    password: ${RABBITMQ_PASSWORD:ecommerce_local}
  mvc:
    problemdetails:
      enabled: true

server:
  port: ${SERVER_PORT:8083}

jwt:
  public-key: ${JWT_PUBLIC_KEY:classpath:keys/public.pem}
  issuer: ${JWT_ISSUER:mini-ecommerce}

catalog-service:
  url: ${CATALOG_SERVICE_URL:http://catalog-service:8082}

app:
  security:
    internal-token: ${INVENTORY_INTERNAL_TOKEN:change-me-in-prod}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
